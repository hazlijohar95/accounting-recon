/**
 * Remotion bundle entry point.
 * This file is the entry for the Remotion CLI and preview server.
 */
import { registerRoot } from 'remotion'
import { RemotionRoot } from './Root'

registerRoot(RemotionRoot)
